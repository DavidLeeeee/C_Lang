# C/C++ 학습 환경

## WSL 설치

Ubuntu 기반 WSL 환경을 사용한다.

```powershell
wsl --install -d Ubuntu
```

## WSL 실행

Ubuntu WSL 실행 명령:

```powershell
wsl.exe -d Ubuntu
```

## 계정 정보

- Username: `david`
- Password: `12qwaszx`

## 메모

- C/C++ 및 시스템 프로그래밍 실습은 Ubuntu WSL 환경을 기준으로 진행한다.
